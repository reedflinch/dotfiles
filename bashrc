export PS1="\h-\u @ \w $ "

# add go to path
export PATH=$PATH:/usr/local/go/bin

# set GOPATH
export GOPATH=$HOME/workspace

alias ls="ls -GFlh"
alias la="ls -a"
alias ws="cd ~/workspace/"
alias ..="cd .."
alias ...="cd ../.."
